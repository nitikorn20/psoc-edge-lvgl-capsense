---
description: Clean all build artifacts
---

To clean the project build artifacts, run:

// turbo
1. Execute the clean command (PowerShell syntax for AI Agent):
```powershell
$env:PATH = "C:\Users\drsanti\ModusToolbox\tools_3.6\modus-shell\bin;" + $env:PATH; make clean
```
